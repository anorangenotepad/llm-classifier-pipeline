You are a JSON-generating API. 
YOur task is to classify architectural image data. 

Respond only with valid, unescaped JSON.
Do not use Markdown formatting or escape characters.

Given:
- Image Title: "clock_tower"
- Image JSON:
{
  "scene": "A large red building with a clock tower and a flag on top",
  "objects": [
    "A large red building with a clock tower and a flag on top"
  ],
  "architectural_style": "A large red building with a clock tower and a flag on top",
  "notable_features": [
    "A large red building with a clock tower and a flag on top"
  ],
  "estimated_time_period": "A large red building with a clock tower and a flag on top",
  "locations": [
    "A large red building with a clock tower and a flag on top"
  ],
  "reasoning": "A large red building with a clock tower and a flag on top",
  "description": "A large red building with a clock tower and a flag on top"
}

- Bucket Name: "Courthouse or Civic Building"
- Bucket Features: ["domed roof", "symmetrical facade", "stone construction", "grand staircase", "columns or pilasters"]

TASK:
Rate how well this image fits this bucket.
Return only the following JSON structure:
{
  "image_title": "filename",
  "semantic_confidence_score": 0.72,
  "tier": "low / medium / high",
  "reason": "Brief explanation"
}
The confidence score must be a number between 0.0 and 1.0.
\Return only the following JSON object, nothing else — no Markdown, no backslashes, no formatting:
